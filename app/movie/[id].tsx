//[id] is a dynamic route in reactNAtive. ie, it will match any path like /movie/1, /movie/2, etc.

import { View, Text } from "react-native";
import React from "react";

const MovieDetails = () => {
  return (
    <View>
      <Text>MovieDetails</Text>
    </View>
  );
};

export default MovieDetails;
